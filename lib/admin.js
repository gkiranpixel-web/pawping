import {createClient} from "@supabase/supabase-js";
export function clients(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const anon=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;const secret=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!anon||!secret)throw new Error("Admin server variables are missing");return {authClient:createClient(url,anon,{auth:{persistSession:false,autoRefreshToken:false}}),adminClient:createClient(url,secret,{auth:{persistSession:false,autoRefreshToken:false}})}}
export async function requireAdmin(req){const token=(req.headers.authorization||"").replace(/^Bearer /,"");if(!token)throw new Error("Unauthorized");const {authClient}=clients();const {data,error}=await authClient.auth.getUser(token);if(error||!data.user)throw new Error("Unauthorized");const allowed=(process.env.ADMIN_EMAIL||"").trim().toLowerCase();if(!allowed||data.user.email?.toLowerCase()!==allowed)throw new Error("Forbidden");return data.user;}